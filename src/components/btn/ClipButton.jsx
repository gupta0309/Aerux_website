import React from "react";
import "./ClipButton.css";

const ClipButton = () => {
  return (
    <div className="clip-btn-wrapper">
      <button className="clip-btn">Copy Address</button>
    </div>
  );
};

export default ClipButton;